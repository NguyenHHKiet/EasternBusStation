<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.booking">

	<select id="findAll" resultType="com.coeding.springmvc.entity.Bookings">
	<![CDATA[
		SELECT * FROM bookings ORDER BY id desc 
		
	]]>
	</select>
	
	<select id="findById" parameterType="int" resultType="com.coeding.springmvc.entity.Bookings">
	<![CDATA[
		select * from bookings where id = #{id}
	]]>
	</select>
	
	<select id="findByUserId" parameterType="int" resultType="com.coeding.springmvc.entity.Bookings">
	<![CDATA[
		select * from bookings where userId = #{userId}
	]]>
	</select>
	
	<!--
	<select id="findAllProductByCategoryId" parameterType="int" resultType="com.coeding.springmvc.entity.Userz">
	<![CDATA[
		SELECT * FROM mydbcoffee.productz
		INNER JOIN
		mydbcoffee.categoryz
		ON mydbcoffee.productz.category_id= mydbcoffee.categoryz._id
		AND mydbcoffee.categoryz._id = #{id}
		ORDER BY price DESC
		
	]]>
	</select>
	 -->
	<insert id="create" parameterType="com.coeding.springmvc.entity.Bookings">
	<![CDATA[
		insert into bookings (noOfPersons,totalCalculated,filterDate,toDestination,fromDestination,BusName,userId,time,fileName,tripStatus)
		values (#{noOfPersons},#{totalCalculated},#{filterDate},#{toDestination},#{fromDestination},#{BusName},#{userId},#{time},#{fileName},#{tripStatus})
	]]>
	<selectKey resultType="int" keyProperty="id" order="AFTER">
	<![CDATA[
	    SELECT LAST_INSERT_ID()
	    ]]>
	  </selectKey>
	</insert>
	
	<update id="update" parameterType="com.coeding.springmvc.entity.Bookings">
	<![CDATA[
		update bookings set noOfPersons = #{noOfPersons},totalCalculated = #{totalCalculated},filterDate = #{filterDate},
		toDestination = #{toDestination},fromDestination = #{fromDestination},BusName = #{BusName},userId = #{userId},
		time = #{time},fileName = #{fileName},tripStatus = #{tripStatus}
		where id = #{id}
	]]>	
	</update>
	
	<delete id="delete" parameterType="com.coeding.springmvc.entity.Bookings">
	<![CDATA[
		DELETE  FROM bookings
  		WHERE id=#{id}
	]]>	
	</delete>
	
</mapper>
